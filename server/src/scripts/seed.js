// Stub — real seeding (departments, demo accounts, attendance/leave history)
// lands in P1 per docs/DATABASE.md Seed plan. Pass --reset for seed:reset.
import { logger } from '../lib/logger.js';

logger.info('seed: not implemented yet (P1) — see docs/DATABASE.md Seed plan');
process.exit(0);
