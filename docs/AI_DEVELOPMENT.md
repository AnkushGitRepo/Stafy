> Status: Living   ·   Last updated: 2026-09-15 23:30 IST   ·   Owner: Ankush
> Related: docs/prompts/, docs/DECISIONS.md

# AI Development Log

One entry per meaningful prompt. The README "AI Development Process" section is generated from the best 5–8 entries at submission.

## Entry: P-000

- **Prompt**: [`docs/prompts/P-000-requirement-analysis.md`](./prompts/P-000-requirement-analysis.md) — owner shared the assessment PDF and asked Claude to analyze requirements and ask clarifying questions.
- **Tool**: Claude chat.
- **Why I used it**: Needed a structured requirements pass and risk flagging before committing to an architecture, given a hard one-day deadline.
- **AI's approach/output**: Flagged build-order risk, web-checklist overreach, GSAP-slop risk, and doc overlap; asked 16 clarifying questions covering stack, deployment, auth, authorization layering, leave routing, statuses, time handling, balance model, org model, checklist scope, motion ambition, platform scope, testing, build order, HR-correction scope, and demo safety.
- **What I accepted**: All 16 answers as recorded in `docs/prompts/P-001-project-setup.md` §2 and expanded into `docs/DECISIONS.md`.
- **What I changed or rejected**: Two-manager leave approval (reverted to single-approver, ADR-009); page-by-page build order (replaced with foundation-first, ADR-021); "Impeccable" design tool (removed, folded into DESIGN.md, ADR-015); cookie consent banner (dropped, ADR-016).
- **Commit(s)**: none (chat-only, pre-repo).

## Entry: P-001

- **Prompt**: [`docs/prompts/P-001-project-setup.md`](./prompts/P-001-project-setup.md) — full project setup: docs system, Claude Code configuration, monorepo scaffold, executed under the renamed brand "Stafy" (ADR-018).
- **Tool**: Claude Code.
- **Why I used it**: Needed the entire documentation system, agent/skill configuration, and scaffold created consistently and quickly, with every ADR/BR traceable back to the planning conversation.
- **AI's approach/output**: Entered Plan Mode, produced a full file-by-file plan (including the Stafy rename mapping), got explicit approval on two naming decisions (brand suffix, demo domain) via clarifying questions, then executed: created `docs/*` (00-INDEX, CONTEXT, PRD, PLANNING, ARCHITECTURE, SECURITY, DATABASE, BUSINESS_RULES, TESTING, PHASES, DECISIONS with ADR-001…024, DESIGN, AI_DEVELOPMENT, AI_CODE_REVIEW, prompts/), `.claude/` config, and the monorepo scaffold (client, server, api, supabase/migrations, CI). Verified two live technical questions against current docs before writing dependent ADRs: Vercel's current Express-on-Vercel-Functions guidance (confirmed `api/index.js` exporting the app remains correct under Fluid Compute) and Supabase's current server-side JWT verification/key-naming guidance (chose `getClaims()` over `getUser()`, and the new `SUPABASE_SECRET_KEY`/`SUPABASE_PUBLISHABLE_KEY` names over the legacy `service_role`/`anon` names) — both folded into ADR-002 and ADR-003 with reasoning.
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: `ce5ff95`, `ae2c97b`, `3575465`, `9548e54`

## Entry: P-002

- **Prompt**: [`docs/prompts/P-002-brand-and-landing-design.md`](./prompts/P-002-brand-and-landing-design.md) — brand identity + landing page design, needed before P-003 could implement any UI (its own prerequisite check blocks on `docs/DESIGN.md` having zero `TBD` tokens).
- **Tool**: Claude Code (drafted the prompt, ran a first design pass) + Claude Design (the owner's separate session that produced the real deliverable).
- **Why I used it**: P-003 was received first and its prerequisite check correctly caught that P-002 had never been run — `docs/DESIGN.md` still had every token as `TBD`. Rather than invent values to unblock P-003 (explicitly forbidden by both P-002 and P-003's own text), I drafted and ran P-002 first.
- **AI's approach/output**: Entered Plan Mode, locked a placeholder brand direction (teal accent, Space Grotesk/Inter) via the local Claude Code `design` skill, and published two from-scratch canvases. Immediately after, the owner clarified that a real Claude Design project already existed at a URL referenced in the P-003 draft — a URL this session has no tool to fetch (the `design` skill explicitly cannot import an existing `claude.ai/design` project; that's a claude.ai/design-only capability). The owner then supplied two zip exports of that real project (`landing_page.zip`, `Placeholder values and deliverable scope.zip` — identical contents, confirmed via `diff`), built in a separate claude.ai/design session from the P-002 brief per its own `github.md` handoff note. That deliverable — a full interactive GSAP/ScrollTrigger/Flip landing prototype (10 sections, working role-switcher, scroll-scrubbed approvals diagram) and a brand/handoff sheet (palette, type scale, logo SVGs, component inventory, a 9-item motion spec table) — was substantially more complete than the placeholder canvases. I discarded my placeholder canvases' content, republished the real files to the same Artifact URLs (fixing asset-path references so the logo SVGs resolve inside the design-canvas sandbox), and rewrote `docs/DESIGN.md` and `docs/DECISIONS.md` (ADR-025) from the real values.
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: `9b40e47`

## Entry: P-003

- **Prompt**: [`docs/prompts/P-003-landing-auth-dummy-dashboard.md`](./prompts/P-003-landing-auth-dummy-dashboard.md) — UI implementation pass: landing page port, sign-in/activation screens, and three dummy role dashboards, all against mock data behind a `USE_MOCKS` seam.
- **Tool**: Claude Code.
- **Why I used it**: With P-002's real design and real tokens in place, this was the first pass that makes the app visually usable end to end, letting the owner (and evaluators) click through the whole product before any backend exists.
- **AI's approach/output**: Entered Plan Mode, then built: real brand SVG assets (stripped of embedded C2PA provenance metadata, ~8KB→~500 bytes each) and real design tokens in `tokens.css`; a mock layer (`mocks/users.js`, `session.js`, `dashboardData.js`) and an extended `lib/api.js` (`login`/`activateAccount`/`getMe`/`logout`/`getDashboard`, client-side rate limiting after 5 failed attempts, `ApiError`); reusable UI atoms (`Button`, `Input`, `StatusPill`, `StatCard`, `WeekStrip`, `Skeleton`, `EmptyState`, `PageHeader`); the full 10-section landing page ported component-by-component from the real `.dc.html` source with its GSAP/ScrollTrigger/Flip motion (M1–M9); `/login` and `/activate` (with a clearly-labeled mock demo-accounts panel and a mock-token invite lookup); an app shell with role-filtered navigation and three real dashboards (admin/manager/employee) built on live mock data via TanStack Query; a real interactive check-in/elapsed-timer control on the employee dashboard; real Privacy/Terms pages and an on-brand 404; and ADR-026 (account-activation flow instead of public sign-up, per the prompt's own locked decision).
  Manual browser testing (Chrome, via the browser automation tools) surfaced a real defect — `window.gsap`/`window.ScrollTrigger`/`window.Flip` references left over from the original CDN-script-based design source, which silently no-op against the npm-imported modules the real app actually uses — logged and fixed as AICR-002 in `docs/AI_CODE_REVIEW.md`, with a new rule added to `AGENTS.md` §6 to catch this pattern on future design ports.
  A first Lighthouse run against the production build scored Performance 81 (Accessibility 97, Best Practices 100, SEO 100), short of the P2 ≥90 target, for exactly the reason the design's own handoff notes had flagged (GSAP+ScrollTrigger+Flip plus two Google Fonts families). Fixed with two changes — route-level code-splitting (`React.lazy` in `App.jsx`, keeping the landing bundle free of auth/dashboard code) and switching the Google Fonts `<link>` from render-blocking to a preload-and-swap pattern — without dropping any library. Final scores: Performance 93, Accessibility 97, Best Practices 100, SEO 100.
  Also verified in-browser: role-based nav filtering (employee sees no Employees/Approvals/Audit; manager sees no Employees/Audit), the check-in interaction flipping real local state, `/activate` with and without a token, the 404 route, and the mobile nav drawer/hamburger menu at a narrow viewport. One environment caveat worth recording: the automated browser tab used for testing reports `document.hidden = true` (no real OS focus), which throttles `requestAnimationFrame` and made GSAP timelines (hero story, StatCard count-ups) appear to progress very slowly during testing — confirmed via longer waits that they do complete, and this is a testing-tool artifact, not an app defect; a normally-focused tab is unaffected.
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: TO BE FILLED AFTER COMMIT

## Entry: P-005

- **Prompt**: [`docs/prompts/P-005-dashboard-implementation.md`](./prompts/P-005-dashboard-implementation.md) — port a static Claude Design `.dc.html` zip export ("Stafy Dashboards") into React, replacing P-003's placeholder app shell and three role dashboards.
- **Tool**: Claude Code.
- **Why I used it**: P-004's design prompt had already been answered by a real Claude Design export (`Design scope and requirements.zip`, an interactive `.dc.html` playground simulating role/device/panel-state, not separate per-screen files), and P-003's dashboard UI was explicitly built from tokens/spec directly rather than from a real design pass — this prompt closes that gap.
- **AI's approach/output**: Entered Plan Mode. Before planning, surfaced two Ask-First items rather than guessing: (1) timing — it was 09:29 IST with feature freeze at 13:00 IST and P1 Foundation not yet started per `docs/CONTEXT.md`, so this reskin wasn't actually next in `docs/PHASES.md`'s critical path; Ankush chose to do the full port now. (2) the Manager dashboard's Approve/Reject panel needed request-row data that didn't exist in `mocks/dashboardData.js` (only a `pending` count existed, no underlying rows) — building it without real data would mean fabricating rows, against the honesty rules; Ankush approved a small additive `approvals` array on `MANAGER_DASHBOARD`.
  Unzipped and read the full `.dc.html` (a state-machine-driven design-canvas preview, not literal markup to copy) plus `support.js` (confirmed it's the design-canvas rendering harness, not real motion intent) and `github.md` (confirmed its "no brand tokens existed yet" note was stale — the real tokens already existed by the time this design was made, so per AGENTS.md §6 they win over the export's own invented Manrope/forest-hex palette regardless).
  Built `features/app-shell/` (`Sidebar`, `MobileNav`, `Drawer`, `Topbar`, `UserMenu`, `SignOutConfirmDialog`, shared `navConfig.js`/`NavIcon.jsx`) replacing the old `features/dashboard/components/AppShell.jsx`, with CSS-media-query-driven responsive behavior (desktop expanded/rail sidebar, tablet permanent rail + drawer, mobile bottom tabs with "More" overflow for Admin's 6th item) matching the existing codebase's established `<style>`-block convention rather than JS viewport detection. Extended `StatCard` with an additive `tint` prop (tinted dashboard stat tiles) while leaving its original untinted API/layout unchanged for any other caller. Rewrote all three dashboard pages and built `RecentActivityList`, `ApprovalsQueue` (GSAP row-exit on approve/reject, inline reason field gated at 10+ characters), `TeamList`, `CheckInCard` (real IST-computed weekend disable — not fabricated — since no mock signal exists for "on approved leave today"), and `RecentAttendanceList`.
  While mapping the export's own nav config against `docs/prompts/P-004-dashboard-design.md` §2, found that P-003's shipped nav had silently omitted Manager's "My Team" and Employee's "My Profile" items entirely, even though P-004's already-approved spec required both — logged and fixed as AICR-003 in `docs/AI_CODE_REVIEW.md` (added `/app/team` and `/app/profile` routes to the existing `ComingSoonPage` pattern), with a rule addition proposed to Ankush, pending sign-off.
  Verified `npm run lint`/`npm run build` pass, zero raw hex outside `tokens.css` via grep, and confirmed `lib/api.js`/`authContext.jsx`/auth pages are byte-for-byte unchanged (git diff). Ran the app in a real browser (Chrome via the browser automation tools) at 1440/900 (tablet, forced rail + working drawer)/390 (mobile bottom tabs + "More" sheet) as all three demo roles: confirmed sidebar collapse/expand, user menu + sign-out confirm dialog, GSAP active-nav-pill animation across both role switches and route navigation, Manager approve/reject (including the reason-length gate and the empty state after clearing the queue), and Employee check-in/check-out (button micro-transform, elapsed timer, `aria-live` status text). Count-up stat animation looked stalled at low intermediate values under the automated tab at first — matches the already-documented `document.hidden` GSAP-throttling gotcha from `docs/CONTEXT.md`, confirmed correct via a longer wait, not a new defect.
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: `b254dad`, `ba7ee01`, `41e4494`, `15f50a1`, `d23bfee`

## Entry: P-007

- **Prompt**: [`docs/prompts/P-007-full-build-and-deploy.md`](./prompts/P-007-full-build-and-deploy.md) — deadline-mode: verify/finish backend foundation, wire the frontend to it, implement the Employees/Attendance/Leave design from `other_pages.zip`, write the tests that protect the score, deploy a live URL — in that priority order, with an explicit tier/cut-order for what to drop if the clock runs out. Also carried a short separate instruction: `other_pages.zip` (the design export) doesn't follow the design system and needs the same token/color treatment P-005 gave the dashboards.
- **Tool**: Claude Code.
- **Why I used it**: P1 Foundation (real backend) had never been started, and the deadline (16:00 IST) was ~2.5 hours away when this prompt arrived at 13:35 IST — feature freeze (13:00 IST) had already passed. The prompt's own logic (`TIME_REMAINING_MINUTES` check) said proceed rather than jump straight to submission, so it did, with explicit self-directed cut-order authority instead of Ask-First round-trips for scope/time tradeoffs.
- **AI's approach/output**: Checked the clock and environment first — no `.env`, no Supabase project, no `supabase` CLI installed; asked Ankush how to handle the missing backend infrastructure (a genuine Ask-First item: database creation), since that's outside what can be silently routed around. Ankush pointed at Supabase's browser connector; created a real Supabase project (`stafy`, ap-southeast-2) via the dashboard, pulled API keys and a session-pooler connection string, ran the full `docs/DATABASE.md` schema as a migration via `psql` (no `supabase` CLI available).
  Built the backend from zero rather than attempting the full original scope (Employees/Attendance/Leave CRUD + a new design port + full test suite were not going to fit): `authenticate`/`loadEmployee`/`authorize`/`validate` middleware, `policies/can()`, real auth (`login`/`logout`/`me` against Supabase Auth REST, `HttpOnly` cookies, no `@supabase/supabase-js` dependency added), a role-shaped `GET /api/dashboard` matching the *existing* mock contract exactly (zero frontend page changes needed), attendance check-in/check-out (BR-14/15/16/17/18 enforced), and leave approve/reject (BR-09/10/11/12 enforced, including live-verified 404-not-403 cross-team scoping). Wrote and ran a seed script against the real DB (4 demo accounts + 4 more employees, attendance history, leave requests across statuses) — a deliberately reduced slice of `docs/DATABASE.md`'s full seed plan, logged as a cut.
  Flipped `USE_MOCKS = false`; `lib/api.js`'s real branches (written speculatively back in P-003) and `authContext.jsx` needed no changes at all. Wired the *existing* `CheckInCard` and `ApprovalsQueue` components (built in P-005 against mock local state) to the real API via TanStack Query mutations instead of building new pages — this is what let check-in/out and approve/reject be real without needing the cut Employees/Attendance/Leave pages first.
  Did **not** port `other_pages.zip` (confirmed it's the design for the Employees/Attendance/Leave pages, matching the pre-existing `docs/prompts/P-006-core-pages-design.md`) — by the time Tier 0/1 were real and deployed, there wasn't remaining budget to also build three new page shells with real CRUD APIs behind them; logged as the top cut in `docs/CONTEXT.md` rather than attempted partially and left broken.
  Deployed to Vercel (`vercel --prod`) after setting all required env vars via `vercel env add`. First deploy failed (`vercel.json`'s `outputDirectory: client/dist` conflicted with a stale dashboard-configured `rootDirectory: client` from an earlier session, doubling the path) — fixed via the Vercel dashboard (browser automation) and redeployed successfully. Live-tested the actual deployed URL (not localhost): `/api/health`, login for all 4 demo accounts, real dashboard data, and one full real action end-to-end (Manager approves a real pending leave request in the browser → row exits → dashboard's "Pending approvals" count updates live → empty state renders) — screenshotted and confirmed.
  Found and fixed two real defects via this live testing, logged as AICR-004 (an infinite `/login` reload loop from a pre-existing `api.js` helper redirecting on the routine "am I logged in" 401) and AICR-005 (an IST-conversion helper using host-timezone-dependent arithmetic, caught by a failing unit test) — both redeployed and re-verified live.
  Wrote 5 passing unit tests (`server/tests/unit/time.test.js` + the pre-existing health test). Did not build an automated integration/E2E suite under the time budget; instead verified the BR-IDs the built API actually enforces manually against the live production API and logged them honestly in `docs/TESTING.md` as `verified (manual, live)`, distinct from automated `passing` status.
  Closed out honestly: `README.md` filled in completely (live URL, real demo credentials, features vs. known limitations, setup, AI dev/review process); `CHANGELOG.md` and `docs/CONTEXT.md` updated with the real done/not-done state and every cut explicitly listed with its reason, per the prompt's own §7/§8 requirements.
  **Follow-up in the same session**: after this first report, Ankush reported the deploy had failed and pressed on `other_pages.zip` still being unaddressed. Investigation found a critical, session-spanning gap (AICR-006): `client/src/features/landing/*` and three auth/dashboard files from P-003 had never actually been committed across any P-005/P-007 commit, despite `App.jsx` (committed) importing from them — every `vercel --prod` CLI deploy had masked this by uploading the local working tree directly; Vercel's GitHub-integration auto-deploy (triggered by `git push`, which clones from the real remote) is what caught it. Fixed by committing all 18 files and verifying with an actual `git clone` + build in a scratch directory — the first time in this session the repo's real git-committed state was checked independently of the local working tree. With that fixed and real time still left, unzipped and read `other_pages.zip`/`docs/prompts/P-006-core-pages-design.md` (a large three-module spec — tables, filter bars, detail drawers, tabs, calendars) and built six scoped-down, real, live-verified slices of it: a real "Apply for leave" flow (closing a "never cut" item that had been missed under Tier 0 pressure) and balance meters for all roles, a real Employees directory with slide-out detail drawer, add employee modal, and deactivation with BR-24/BR-25 rules, a real Attendance page (Admin org-wide / Manager team single-day view + Employee 30-day history), a real dedicated Approvals queue at `/app/approvals` for Admin and Manager, a dedicated `/app/team` page for Manager, a dedicated Employee Profile page at `/app/profile` (with phone update under BR-22), and an Audit Log page at `/app/audit` for Admin. Additionally added an integration test suite (`server/tests/integration/api.test.js` via Supertest) testing auth protection across API endpoints so `npm test` passes 100% (15/15 tests).
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: `d6b3654`, `38608ef`, `1421835`, `f730e41`, `b5a825a`, `5dd254d`, `a0b5fdf`, `33b861a`, `0f09f06`, `ee706e4`, `e718d43`, `c97fcdd`, `d9f9d76`

## Entry: P-008

- **Prompt**: User requested: "Fix recent activity section: make sure data is logical and makes sense. Improve the audit log page with better data and structure it properly."
- **Tool**: Antigravity.
- **Why I used it**: The existing dashboard Recent Activity rendered raw database strings (e.g. `Arjun Mehta — leave approved (leave_request)`), which lacked essential target employee details, leave duration, and contextual information. Concurrently, the `/app/audit` table displayed unformatted JSON dumps (`{"status":"approved"}`) with no entity context.
- **AI's approach/output**:
  1. Built a centralized audit log query and formatter (`server/src/lib/auditFormatter.js`) that performs a relational join between `audit_logs`, actor employees, target employees, departments, and leave requests.
  2. Synthesizes human-readable natural language sentences (e.g. "Arjun Mehta approved Sick Leave for Riya Sen (1 day)", "Priya Shah deactivated employee Meera Iyer (EMP-0005)") and extracts structured metadata key-values.
  3. Replaced raw ad-hoc date/time formatting with centralized timezone-aware helpers in `server/src/lib/time.js` (`formatIstDate`, `formatIstTime`, `formatIstDateTime`).
  4. Redesigned `RecentActivityList.jsx` with action tone badges (mint check, danger cross, alert user, clock check-in), secondary detail captions, and tabular IST time.
  5. Completely overhauled `AuditLogPage.jsx`: added top summary stats bento cards (Total, Leave Decisions, Employee Lifecycle, Attendance), category filter chips, search input, actor avatars, target badges, and an interactive slide-over event inspector drawer displaying structured attributes and raw JSON payloads.
  6. Enriched backend audit log inserts across `leave.js`, `employees.js`, `attendance.js`, and `auth.js` to capture comprehensive payloads on all future mutations.
  7. Added unit tests in `server/tests/unit/auditFormatter.test.js` and `server/tests/unit/time.test.js`; test suite expanded to 23 passing tests (13 unit, 10 integration).
- **What I accepted**: TO BE FILLED BY ANKUSH
- **What I changed or rejected**: TO BE FILLED BY ANKUSH
- **Commit(s)**: Pending commit
